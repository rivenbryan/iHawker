
import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
    palette: {
      primary: {
        main: '#FFC137',
      },
    },
    typography: {
      fontFamily: '"Inter"',
    },
  });

  export default Theme